{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "echo \"yes\" && exit 1",
    "format": "prettier --write \"src/**/*.js\"",
    "lint": "prettier --check \"src/**/*.js\""
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^5.15.1",
    "@fxi/el": "^0.1.6",
    "@mapbox/mapbox-gl-geocoder": "^4.7.0",
    "ajv": "^7.0.3",
    "copy-text-to-clipboard": "^2.2.0",
    "mapbox-gl": "^2.0.0",
    "suncalc": "^1.8.0"
  },
  "devDependencies": {
    "prettier": "^2.0.5",
    "snowpack": "^2.17.0"
  }
}
