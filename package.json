{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "echo \"What is test?.\" && exit 1",
    "format": "prettier --write \"src/**/*.js\"",
    "lint": "prettier --check \"src/**/*.js\""
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^5.15.1",
    "ajv": "^7.0.3",
    "fontawesome": "^5.6.3",
    "mapbox-gl": "^2.0.0"
  },
  "devDependencies": {
    "prettier": "^2.0.5",
    "snowpack": "^2.17.0"
  }
}
